<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
      html,
      body {
        margin: 0;
        padding: 0;
      }

      header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        background-color: aliceblue;
      }

      header h1 {
        margin-left: 20px;
      }

      header a {
        margin-right: 20px;
      }

      ul {
        margin: 0;
        text-align: center;
        position: absolute;
        background-color: whitesmoke;
        left: 50%;
        transform: translateX(-50%);
        /* margin-top: 2em; */
        padding: 0;
        padding: 2em 2em;
      }

      li {
        width: 200px;
        height: 250px;
        border-radius: 30px;
        display: inline-block;

        font-size: 1em;
        background-color: white;
        /* top: 50%; */
        box-shadow: 0 1px 5px rgba(0, 0, 0, .2);
      }

      li p,
      li b {
        margin-top: 1em;
        display: flex;
        flex-direction: column;
        gap: 1em;
      }
    </style>
  </head>
  <body>
    <header>
      <h1>댓글 모음</h1>
      <a href="/">홈으로 이동하기</a>
    </header>
    <ul>
      <% for(let i = 0; i < commentInfos.length; i++) {%>
      <li>
        <b><%= commentInfos[i].userId %></b>
        <p><%= commentInfos[i].comment %></p>
        <p><%= commentInfos[i].date %></p>
      </li>
      <% } %>
    </ul>
  </body>
</html>